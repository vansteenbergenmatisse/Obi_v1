# 04 · CI inventory (`.github/`) for the 1.1 folder move

READ-ONLY inventory. Source: `.github/workflows/ci.yml`.

## Files under `.github/`

- `.github/workflows/ci.yml` — the only file. No other workflows, no `dependabot.yml`, no issue/PR templates, no `CODEOWNERS`.

## Lines naming a changing path

Move map: `apps/automation` → `backend`; compose `infra/foundation/docker-compose.yml` → `knowledge-base/local/docker-compose.yml`; pnpm workspace `apps/web` → `frontend`.

| ci.yml line | Text | Change |
|---|---|---|
| `ci.yml:23` | `run: cd apps/automation && uv venv --python 3.12 && uv pip install -e ".[dev]"` | `cd apps/automation` → `cd backend` |
| `ci.yml:31` | `cd apps/automation` | `cd apps/automation` → `cd backend` |

### Notes on paths that do NOT need changing

- No `working-directory:` keys anywhere (all cwd changes are inline `cd`).
- No compose path: `infra/foundation/docker-compose.yml` is not referenced in CI (`make test-db` etc. resolve compose via the root Makefile, not here).
- No artifact upload/download paths.
- No explicit `pnpm --filter web` invocation; `pnpm install --frozen-lockfile` (`ci.yml:57`) runs at repo root and does not name `apps/web`.
- No explicit cache-key referencing `uv.lock` / `pnpm-lock.yaml`: uv uses `setup-uv@v5` defaults; Node uses `cache: "pnpm"` (`ci.yml:54`) which auto-discovers the root lockfile. Neither names a changing path.

**Path lines needing change: 2** (`ci.yml:23`, `ci.yml:31`).

## Collected-test-count FLOOR (must bump in 1.1.2)

The `--collect-only` guard requiring `>=` the floor. Verbatim:

- `ci.yml:29`: `      - name: Coverage floor (>= 621 tests collected)`
- `ci.yml:32`: `          count=$(uv run pytest --collect-only -q | tail -1 | grep -oE '^[0-9]+')`
- `ci.yml:34`: `          if [ -z "$count" ] || [ "$count" -lt 621 ]; then`
- `ci.yml:35`: `            echo "::error::pytest collected $count tests, below the substep 0.5.1 coverage floor of 621 (harness.md)"`

The load-bearing guard is **`ci.yml:34`** (`[ "$count" -lt 621 ]`). The number `621` also appears in the step name (`ci.yml:29`) and the error message (`ci.yml:35`); all three must be bumped together when 1.1.2 adds tests.

**Current floor: 621.**
