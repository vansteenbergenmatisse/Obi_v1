# Agent research

Scratch notes from ad hoc research, kept separate from the authoritative design material one
level up. Nothing in here is a source of truth — if a finding here needs to stick, it belongs in
`docs/adr/` (a decision) or `docs/plan/` (a status/delta), not here.

Currently empty. That's expected, not a mistake — files land here only when research work
actually happens.
