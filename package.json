{
  "name": "rag-toast-omniboost",
  "private": true,
  "version": "0.1.0",
  "description": "Accuracy-first, Confluence-native RAG chatbot (Application Platform)",
  "packageManager": "pnpm@10.33.2",
  "workspaces": [
    "apps/web",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
