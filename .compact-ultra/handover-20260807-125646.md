══════════════════ HANDOVER ══════════════════
Generated:    2026-08-07T10:56:46Z
Working dir:  /Users/matissevansteenbergen/Downloads/AGENTIC WORKLFOWS/OMNIBOOST/RAG-TOAST-omniboost
Branch:       main  ·  tip: feaafcd refactor(tests): route test_retrieval_eval through evaluation/retrieval facades
Commit:       feaafcd59cc9fcff37377c7dadc9e1a63b54cc13
Files:        ~180  ·  uncommitted: 0 (working tree clean)
═══════════════════════════════════════════════

GOAL:        Enforce the CLAUDE.md architecture standard on the Omniboost RAG monorepo — every cross-feature import goes through a public feature root, machine-enforced; author the missing project standard; rewrite the handover. Executing a fixed 8-phase plan (0–7), stopping every 2 phases to check in.
PROGRESS:    Phases 0,1,2,3 done and committed (10 commits, tags pre-refactor-baseline + green-baseline). All 4 features now have ZERO external deep-imports.
WORKING:     apps/automation pytest 99 passed; import app.main clean (no cycle); OpenAPI byte-identical to baseline; 4/4 feature facades; Postgres omniboost_rag_pg on :5434.
BROKEN:      Nothing broken. Pre-existing (baseline, NOT regressions): ruff check 2 errors (alembic import-sort), ruff format 25 files, pyright 31 errors/1 warning. Gate policy = no-regression on these, not zero.
IN PROGRESS: Nothing mid-edit. Just finished Phase 3 (commit feaafcd) and paused at the Phase 2&3 check-in.
BLOCKED ON:  User approval to proceed with Phases 4 & 5 (they asked to compact first; said to LEAVE the master handover alone — do NOT rewrite MASTER-HANDOVER.md until Phase 7).

═══ Conversation Digest ═══

[USER] Execute the "Architecture Conformance Refactor" plan and stop every 2 phases to check in.
[LLM]  Loaded executing-plans skill; ran 3 parallel Explore agents to verify the plan's structural/symbol claims against the real repo before touching anything.
[Δ]    none (read-only verification)

[USER] (same request — plan was pasted in full: Phases 0–7 + verification + risk register)
[LLM]  Confirmed plan premises; corrections: evaluation/__init__.py had an 86B docstring (not 0B); datasets_dir() had 3 consumers not 2; root .env (not apps/automation/.env) supplies CONFLUENCE_WEBHOOK_SECRET; ~/Developer/omniboost-rag in a traceback was a stale pytest bytecode cache (repo was moved), cleared it.
[Δ]    none

[LLM]  Phase 0: captured baseline (pytest 1 failed/98 passed, 99 collected, OpenAPI, pyright 31/1, ruff 2+25, import-graph 45) to scratchpad; deleted 0-byte mis-cased .Claude/; fixed .gitignore (track eval-reports floor); committed red state + tag.
[Δ]    e10ee9d + tag pre-refactor-baseline

[LLM]  Phase 1: hermetic settings fixture (model_copy strips webhook creds — verified all 10 consumers safe); repaired turbo.json (stripped 5 stray "read " bytes). Gate 99 passed.
[Δ]    325dd8f + tag green-baseline

[LLM]  Phase 2: added app/features/__init__.py (gated on node-id SET identity); git mv hashing.py -> app/shared/hashing.py + shared contract __init__ + rewrote 5 importers.
[Δ]    86f3c9e, d03ffeb

[LLM]  Caught a staging slip: first 2b commit captured only the rename (git add aborted on stale path). Amended so rename + __init__ + 5 edits are one atomic commit.
[Δ]    d03ffeb (amended)

[LLM]  Phase 3: built 4 feature facades leaf-first (evaluation, retrieval, ingestion, confluence_sync), promoted reusable_active_children, routed all consumers + test_retrieval_eval through roots. worker.py:67 left untouched (cycle-breaker).
[Δ]    c2f4674, e8224e2, 5bf0db5, 9762015, af7c6a6, feaafcd

[LLM]  Discovered baseline dirtier than plan assumed (ruff/pyright not clean). Applied plan's pyright rule (same-counts) uniformly to ruff: gate = no-regression, do NOT reformat 25 unrelated files. To be logged in ADR-0003.
[Δ]    (policy decision)

[LLM]  invariant-3 (app.* modules after import app.main) is now 46 not 45; +1 = app.shared namespace from Phase 2b relocation; confirmed no facade leaks into startup graph.
[Δ]    (verified benign)

[USER] compact-ultra this, leave the master handover alone.
[LLM]  Ran compact-ultra skill (this handover). Did NOT touch MASTER-HANDOVER.md.
[Δ]    .compact-ultra/handover-*.md

═══ Project Map ═══

apps/automation/                         — Python 3.12 FastAPI RAG service (the ONLY app touched so far). uv + hatchling. This is where the refactor lives.
  app/
    main.py                              — FastAPI entrypoint; NOW imports confluence_sync via facade (rewritten af7c6a6). Reports version 0.2.0.
    __init__.py                          — empty 0B (Phase 6 will put __version__ here)
    features/
      __init__.py                        — ADDED 86f3c9e (makes features a real package; docstring only)
      FEATURES.md                        — per-feature doc; Phase 7 must replace "Public surface" fields with real __all__
      evaluation/__init__.py             — FACADE (c2f4674): evaluate, load_dataset, load_corpus_loader, confluence_fixtures_dir, datasets_dir, RankFn, Eval{Case,Dataset,Report,Result}
      evaluation/fixtures.py             — added datasets_dir() (c2f4674)
      retrieval/__init__.py              — FACADE (e8224e2): HybridRetriever, PrincipalPermissionPolicy. NOT wired into main.py; only test-exercised.
      ingestion/__init__.py              — FACADE (9762015): build_ingestion_services, PageHashes, deactivate_page, stage_and_activate, reusable_active_children, rollback_to, ChangeClass, TargetVersions, classify, decide_body_fetch, map_page_status, get_local_state, normalization (module re-export)
      ingestion/application/versioning.py— reusable_active_children promoted from private (5bf0db5); _resolve_children stays private
      confluence_sync/__init__.py        — FACADE (af7c6a6): router, drain, reap, run_reconciliation, KIND_LIGHTWEIGHT, KIND_COMPLETE, SlidingWindowRateLimiter
      confluence_sync/server/__init__.py — sub-facade; added SlidingWindowRateLimiter (af7c6a6)
      confluence_sync/application/worker.py — worker.py:67 function-local import of reconciliation = DO NOT TOUCH (cycle-breaker)
      confluence_sync/application/sync_service.py, reconciliation.py — consumers rewritten to import ingestion via root
      confluence_sync/tests/test_retrieval_eval.py — routed through facades + datasets_dir() (feaafcd)
      confluence_sync/tests/conftest.py  — hermetic settings fixture (325dd8f); owns Postgres DB setup (_configure_test_engine, session-scoped)
    shared/
      __init__.py, hashing.py            — hashing relocated here from platform (d03ffeb); contract: no app.features/app.platform imports
    platform/
      clients/__init__.py                — EMPTY 0B — Phase 4 target (build facade; contextualizer.py:18-22 imports ONLY anthropic symbols, so gather full export list from all 9 call sites)
      db/                                — deliberately NOT migrated (ADR-0003 D6: large intrinsically-namespaced vocabulary)
    conftest.py                          — root test conftest (env setdefault only, no DB)
    pyproject.toml                       — version 0.1.0 (disagrees w/ FastAPI 0.2.0; Phase 6 single-sources)
    turbo.json                           — repaired (325dd8f)
  Makefile                               — has `make eval`; Phase 5 adds `make boundaries` + `make check`
apps/web/                                — Next.js frontend (untouched; Phase 6 adds tailwind globs, Phase 7 chat/FEATURES.md)
docs/adr/                                — 0001, 0002 exist; Phase 7 writes 0003
package.json                             — has redundant "workspaces" array (Phase 6 deletes; pnpm uses pnpm-workspace.yaml)
CLAUDE.md                                — DOES NOT EXIST yet; Phase 7 authors the project standard here
MASTER-HANDOVER.md                       — exists in .compact-ultra/ or root; Phase 7 rewrites it — USER SAID LEAVE IT ALONE FOR NOW
.compact-ultra/                          — session handovers (tracked in git per plan; not ignored)

Scratchpad baseline (invariants, gitignored, session-local):
/private/tmp/claude-501/-Users-matissevansteenbergen-Downloads-AGENTIC-WORKLFOWS-OMNIBOOST-RAG-TOAST-omniboost/625971f6-1aae-4c67-9864-f8fc971d873d/scratchpad/baseline/
  SUMMARY.md, openapi.json, collect.txt, nodeids_ref.txt, format-files.txt, pytest.txt, pyright.txt

═══ Diff Summary ═══

RECENT COMMITS
feaafcd refactor(tests): route test_retrieval_eval through evaluation/retrieval facades
af7c6a6 refactor(confluence_sync): add public surface facade, route app.main through it
9762015 refactor(ingestion): add public surface facade, route consumers through it
5bf0db5 refactor(ingestion): promote reusable_active_children to public name
e8224e2 refactor(retrieval): add public surface facade
c2f4674 refactor(evaluation): add public surface facade + datasets_dir()
d03ffeb refactor: relocate hashing to app/shared (inhabit the shared role)
86f3c9e refactor: add app/features/__init__.py so features is a real package
325dd8f fix: trustworthy green — hermetic settings fixture + repair turbo.json
e10ee9d chore: initial baseline (red state) as bisect anchor

TAGS: pre-refactor-baseline (e10ee9d, original tree) · green-baseline (325dd8f, pre-Phase-2)

UNCOMMITTED
(working tree clean)

LINE CHANGES
(none — everything committed)

═══ Decisions (WHY not in code) ═══

[D1] Gate = no-regression, NOT "all passed", for ruff AND pyright.
     Why: baseline was already dirty (ruff 2 errors+25 unformatted, pyright 31/1) — the plan wrongly assumed near-clean. Plan already treats pyright as "same counts"; applied same rule to ruff rather than reformat 25 unrelated files (scope creep, muddies behavior-unchanged diff). Touched files ARE made clean; whole-repo counts only go down.
     Affects: every phase gate. Must be recorded in ADR-0003 deviations register (Phase 7).

[D2] Committed to main (not a feature branch), committing the RED state first.
     Why: repo had ZERO commits — main is the natural home for initial history; the plan's tag/bisect/rollback design (pre-refactor-baseline..HEAD) needs linear history here. Red state committed intentionally as a bisect anchor.
     Affects: rollback = git reset --hard <tag>.

[D3] Never-import-your-own-root is the #1 correctness risk; facades use eager exports.
     Why: `from app.features.<f> import X` inside <f> raises ImportError on a half-built package. Verified no feature imports its own root before writing facades. worker.py:67's function-local deep import MUST stay (hoisting it = import-time failure).
     Affects: all 4 facades; the standing import-proof gate.

[D4] ingestion facade re-exports `normalization` as a MODULE, not loose symbols.
     Why: call sites use norm.Block / norm.normalize_body / norm.content_hash / norm.structure_hash — numpy.linalg pattern keeps call-site diff to one import line, zero body churn. Ruff keeps aliased `... as norm` / `... as confluence_router` in their own import statement, so "collapse 5->1" is really 5->2.
     Affects: sync_service.py, main.py.

[D5] platform/db is NOT getting a facade (Phase 4).
     Why: ~40 ORM classes/enums/DDL helpers = large intrinsically-namespaced vocabulary; one namespace = god-module and destroys db.enums.PageStatus vs db.models.PageSource disambiguation. Standard mandates entrypoints for FEATURES, not platform sub-capabilities. ADR-0003 D6.
     Affects: Phase 4 scope, ADR-0003.

[D6] invariant-3 baseline is 46 (was 45).
     Why: +1 = app.shared package namespace from the Phase-2b hashing relocation (app.shared + app.shared.hashing replaced app.platform.hashing). Confirmed NO feature facade appears in app.main's startup graph.
     Affects: later import-graph gate expectations.

═════ RESUME PROMPT ═════

You are continuing an architecture-conformance refactor of the Omniboost RAG monorepo at:
/Users/matissevansteenbergen/Downloads/AGENTIC WORKLFOWS/OMNIBOOST/RAG-TOAST-omniboost

GOAL: Enforce the CLAUDE.md architecture standard — every cross-feature import through a public feature root, machine-enforced; author the project standard; rewrite the handover. Executing a fixed 8-phase plan (0–7), pausing every 2 phases to check in with the user.
BRANCH: main  ·  tip: feaafcd59cc9fcff37377c7dadc9e1a63b54cc13 (Phase 3 complete)

STATE:
  Done:        Phases 0,1,2,3. All 4 features (evaluation, retrieval, ingestion, confluence_sync) have ZERO external deep-imports. Tags: pre-refactor-baseline, green-baseline.
  In progress: none (paused at Phase 2&3 check-in).
  Blocked on:  User approval to start Phases 4 & 5. USER EXPLICITLY SAID: leave MASTER-HANDOVER.md alone (do not rewrite it until Phase 7).

NEXT ACTION (executable), Phase 4 then 5:
  1. Phase 4: build apps/automation/app/platform/clients/__init__.py facade. Export list is NOT just contextualizer.py:18-22 (that imports only anthropic). Gather the real set from all ~9 non-test call sites (grep 'app.platform.clients.' outside platform/clients/). Plan's target set: ConfluenceGateway, HttpConfluenceClient, FixtureConfluenceGateway, EmbeddingProvider, build_embedding_provider, AnthropicMessagesClient, ConfluencePageMeta (VERIFY against call sites). Migrate the sites. Do NOT facade platform/db (D5). Gate, commit.
  2. Phase 5: write tools/check_feature_boundaries.py (stdlib ast only, ~70 lines) enforcing 4 rules ruff can't: (a) code outside app/features/<f>/ may import app.features.<f> but nothing deeper; (b) code inside <f> may deep-import <f> but only import OTHER features at their root; (c) code inside <f> must not import app.features.<f> itself (self-facade trap); (d) platform/** and shared/** must not import features/**, shared/** must not import platform/**. Use ast.walk (catches function-local imports). Wire `make boundaries` + new `make check`. Gate (script exits 0), commit.
  3. STOP and check in with user (every-2-phases cadence). Then Phase 6 (hygiene) & 7 (docs: author root CLAUDE.md, ADR-0003, fix FEATURES.md, rewrite MASTER-HANDOVER.md).

STANDING GATE (run from apps/automation after every phase; all via `uv run`):
  ruff check <touched> AND ruff format --check <touched>  -> clean on touched files
  whole-repo: ruff check . == 2 errors, ruff format --check . <= 25 files, pyright == 31 errors/1 warning  (NO-REGRESSION, see D1)
  python -c "import app.features.evaluation, app.features.retrieval, app.features.ingestion, app.features.confluence_sync, app.main"  -> silent exit 0 (cycle proof)
  pytest -q  -> 99 passed
  pytest --collect-only -q | grep :: | sort  -> byte-identical to scratchpad/baseline/nodeids_ref.txt
  OpenAPI byte-identical to scratchpad/baseline/openapi.json until Phase 6 step 17 (version single-source) changes ONLY info.version
  import-graph: python -c "import app.main,sys;print(len([m for m in sys.modules if m.startswith('app.')]))" -> 46
  python3 tools/check_feature_boundaries.py -> exit 0 (Phase 5+)

KEY FILES (Read on demand):
  - apps/automation/app/platform/clients/__init__.py — EMPTY; Phase 4 target
  - apps/automation/app/features/*/__init__.py — the 4 facades already built (pattern to follow)
  - apps/automation/app/features/confluence_sync/application/worker.py:67 — DO NOT TOUCH (cycle-breaker)
  - apps/automation/Makefile — add `make boundaries` + `make check`
  - docs/adr/0002-*.md — shape to match for ADR-0003 (Phase 7)
  - /private/tmp/claude-501/.../scratchpad/baseline/ — SUMMARY.md + invariants (nodeids_ref.txt, openapi.json)

RUN COMMANDS (from apps/automation):
  test: uv run pytest -q      lint: uv run ruff check .      types: uv run pyright      eval: make eval
  build (repo root): pnpm --filter web build ; node -e "JSON.parse(require('fs').readFileSync('turbo.json','utf8'))"
  lang: Python 3.12 (uv 0.12.1, ruff 0.16.1, hatchling); Node/pnpm 10 for apps/web

ENV (keys, not values):
  - Root .env (repo root, gitignored) — read by get_settings(); has CONFLUENCE_WEBHOOK_SECRET, DATABASE_URL, OPENAI_API_KEY, EMBEDDING_* — set? y
  - DATABASE_URL -> Postgres omniboost_rag_pg on :5434; tests use omniboost_rag_test DB (conftest rewrites URL)

CONVENTIONS:
  commit-style: conventional (refactor(scope): ... / fix: ... / chore: ...); footer "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
  branch: main, no PR requested; one atomic commit per plan sub-step; gate before each commit
  gate: no-regression on pre-existing ruff/pyright dirt (D1), NOT zero

IN-FLIGHT EDIT: none (Phase 3 fully committed, working tree clean)

REJECTED PATHS (don't redo):
  - Don't reformat the 25 pre-existing ruff-unformatted files or fix the 2 alembic import-sort errors (out of scope; no-regression only) — D1
  - Don't rename apps/automation/app/ -> src/ (breaks hatch/pyright/pytest/uvicorn/alembic; Brownfield Preservation)
  - Don't facade platform/db (D5); don't create packages/ui or packages/config (one consumer)
  - Don't hoist worker.py:67's function-local import (import-time failure)
  - Don't rewrite MASTER-HANDOVER.md yet — user said leave it (it's Phase 7 anyway)

RUNNING / EXTERNAL:
  bg processes: none of ours. Docker Postgres omniboost_rag_pg :5434 must be up for pytest (DB conftest).
  ports: :5434 pg (this project), :5433 other pg, :3003 unrelated
  external: OpenAI embeddings (OPENAI_API_KEY); Confluence token is dead/needs rotation (not needed for tests — fixture gateway offline)

═════════════════════════
