══════════════════ HANDOVER ══════════════════
Generated:    2026-08-07 (rewritten after the refactor completed)
Working dir:  /Users/matissevansteenbergen/Downloads/AGENTIC WORKLFOWS/OMNIBOOST/RAG-TOAST-omniboost
Branch:       main  ·  tip: cfdb503 chore: track the Phase-2&3 checkpoint session handover
Commit:       cfdb503ce3c480fb9e266463e71f713bdcb7577a
Files:        165 tracked  ·  uncommitted: 0 (working tree clean)
═══════════════════════════════════════════════

GOAL:        Enforce the CLAUDE.md architecture standard on the Omniboost RAG monorepo — every
             cross-feature import goes through a public feature root, machine-enforced; author the
             missing project standard; rewrite the handover. Fixed 8-phase plan (0–7).
PROGRESS:    ALL 8 PHASES DONE (0–7). 15 commits on main (e10ee9d..cfdb503). Tags
             pre-refactor-baseline + green-baseline. The refactor is complete and behavior-unchanged.
WORKING:     pytest 99 passed; `make check` PASS; boundaries checker exit 0; import app.main clean
             (no cycle); OpenAPI byte-identical to baseline (info.version 0.2.0); collect-only
             byte-identical; app.* startup graph = 46 modules. Web builds (pnpm --filter web build).
BROKEN:      Nothing. Pre-existing baseline dirt held at no-regression (NOT regressions): ruff
             check 2 errors (alembic import-sort), ruff format 25 files, pyright 31 errors/1 warning.
IN PROGRESS: Nothing mid-edit. Working tree clean.
BLOCKED ON:  Nothing for this refactor. NEXT product step (Phase 4) is blocked on a live Confluence
             service account (token dead — no seat) + a first real ingest.

═══ Conversation Digest ═══

[USER] Read the Phase-2&3 checkpoint handover and continue.
[LLM]  Resumed at Phase 3 complete (feaafcd). Verified git state matched; loaded executing-plans.
[Δ]    none (read-only verification)

[USER] go ahead with phases 4 & 5
[LLM]  Phase 4: built app/platform/clients/__init__.py facade. Gathered the REAL export set from all
       call sites — 9 symbols, not the plan's 7 (contextualizer also needs AnthropicError +
       cached_system_block). Routed all 14 external call sites through the root; internal
       clients/ modules + their unit tests keep deep imports. Gate green.
[Δ]    a930f3e

[LLM]  Phase 5: wrote tools/check_feature_boundaries.py (stdlib ast, ast.walk → catches
       function-local imports) enforcing 4 rules ruff can't. VERIFIED adversarially: planted a
       violation of each rule (a via a function-local import, b, c, d×2) — all caught; a legal
       same-feature-deep + other-feature-root control passed; clean tree exits 0. Wired
       `make boundaries` + `make check` (boundaries + tests). Fixed 4 self-introduced ruff errors
       in the tool to hold no-regression.
[Δ]    754b71c

[USER] go ahead with phases 6 & 7
[LLM]  Phase 6: single-sourced the version at app/__init__.py::__version__ (0.2.0), pyproject
       dynamic via Hatchling, app.main stamps FastAPI — OpenAPI stayed byte-identical. Dropped the
       redundant npm `workspaces` array from root package.json (pnpm resolves web from
       pnpm-workspace.yaml — verified). Added the components/ tailwind glob. Web build passes.
       Gotcha: a bare `uv sync` pruned the dev tools; restore with `uv sync --extra dev`.
[Δ]    b380193

[LLM]  Phase 7: authored root CLAUDE.md (project standard), docs/adr/0003 (decision + D1–D6
       register), updated FEATURES.md public-surface fields to the real __all__, and rewrote
       .compact-ultra/MASTER-HANDOVER.md from a pre-refactor audit to a completed record.
[Δ]    fe4bb83, cfdb503

═══ Project Map ═══

apps/automation/                         — Python 3.12 FastAPI RAG service. uv + Hatchling. Where the refactor lives.
  app/
    __init__.py                          — __version__ = "0.2.0" — SINGLE SOURCE (pyproject dynamic reads it; main.py stamps FastAPI)
    main.py                              — FastAPI entrypoint; imports confluence_sync + platform.clients via facades; version=__version__
    features/
      __init__.py                        — makes features a real package (86f3c9e)
      FEATURES.md                        — per-feature doc; public-surface fields now list the REAL __all__ (fe4bb83)
      evaluation/__init__.py             — FACADE: evaluate, load_dataset, load_corpus_loader, confluence_fixtures_dir, datasets_dir, RankFn, Eval{Case,Dataset,Report,Result}
      retrieval/__init__.py              — FACADE: HybridRetriever, PrincipalPermissionPolicy
      ingestion/__init__.py              — FACADE: build_ingestion_services, stage_and_activate, deactivate_page, rollback_to, reusable_active_children, get_local_state, classify, decide_body_fetch, map_page_status, ChangeClass, PageHashes, TargetVersions, normalization (module re-export, D4)
      confluence_sync/__init__.py        — FACADE: router, run_reconciliation, drain, reap, KIND_LIGHTWEIGHT, KIND_COMPLETE, SlidingWindowRateLimiter
      confluence_sync/application/worker.py — worker.py lazy import of reconciliation (same-feature deep, legal under rule b) = DO NOT HOIST (cycle-breaker)
    shared/
      __init__.py, hashing.py            — hashing relocated here from platform (d03ffeb); contract: no app.features/app.platform imports
    platform/
      clients/__init__.py                — FACADE (a930f3e): 9 symbols — ConfluenceGateway, HttpConfluenceClient, FixtureConfluenceGateway, ConfluencePageMeta, EmbeddingProvider, build_embedding_provider, AnthropicMessagesClient, AnthropicError, cached_system_block
      db/                                — deliberately NOT faceted (ADR-0003 D5: large intrinsically-namespaced ORM vocabulary; import by full path)
    pyproject.toml                       — [project] dynamic=["version"]; [tool.hatch.version] path="app/__init__.py"; dev tools in the `dev` extra
  tools/check_feature_boundaries.py      — the boundary gate (stdlib ast). 4 rules. Run: uv run python tools/check_feature_boundaries.py
apps/web/                                — Next.js frontend (untouched by logic; tailwind content += ./src/components/**). Phase 4 owns the chat UI.
docs/adr/                                — 0001, 0002, and NEW 0003-Feature-Boundary-Enforcement.md
Makefile (repo ROOT)                     — added `make boundaries` + `make check`; uses $(AUTOMATION)=apps/automation
package.json (root)                      — redundant "workspaces" array REMOVED; pnpm-workspace.yaml is the source of truth
CLAUDE.md (root)                         — NEW: the governing project standard (authored Phase 7)
.compact-ultra/MASTER-HANDOVER.md        — rewritten to reflect the completed refactor

Scratchpad baseline (invariants, gitignored, session-local):
/private/tmp/claude-501/-Users-matissevansteenbergen-Downloads-AGENTIC-WORKLFOWS-OMNIBOOST-RAG-TOAST-omniboost/625971f6-1aae-4c67-9864-f8fc971d873d/scratchpad/baseline/
  SUMMARY.md, openapi.json, collect.txt, nodeids_ref.txt, format-files.txt, pytest.txt, pyright.txt

═══ Diff Summary ═══

COMMITS THIS REFACTOR (e10ee9d..cfdb503, all on main)
cfdb503 chore: track the Phase-2&3 checkpoint session handover
fe4bb83 docs: author project standard, ADR-0003, and refresh feature/handover docs   ← Phase 7
b380193 chore: repo hygiene — single-source version, drop redundant pnpm workspaces  ← Phase 6
754b71c feat(tools): machine-enforce feature/platform/shared import boundaries        ← Phase 5
a930f3e refactor(platform/clients): add public surface facade, route consumers        ← Phase 4
feaafcd refactor(tests): route test_retrieval_eval through evaluation/retrieval facades ┐
af7c6a6 refactor(confluence_sync): add public surface facade, route app.main through it │
9762015 refactor(ingestion): add public surface facade, route consumers through it     │ Phase 3
5bf0db5 refactor(ingestion): promote reusable_active_children to public name           │ (prior
e8224e2 refactor(retrieval): add public surface facade                                 │ session)
c2f4674 refactor(evaluation): add public surface facade + datasets_dir()               ┘
d03ffeb refactor: relocate hashing to app/shared (inhabit the shared role)             ← Phase 2
86f3c9e refactor: add app/features/__init__.py so features is a real package           ← Phase 2
325dd8f fix: trustworthy green — hermetic settings fixture + repair turbo.json         ← Phase 1
e10ee9d chore: initial baseline (red state) as bisect anchor                           ← Phase 0

TAGS: pre-refactor-baseline (e10ee9d, original tree) · green-baseline (325dd8f, pre-Phase-2)

UNCOMMITTED: (working tree clean)

═══ Decisions (WHY not in code) — recorded in ADR-0003 ═══

[D1] Gate = no-regression, NOT "all passed", for ruff AND pyright.
     Baseline was already dirty (ruff 2 errors + 25 unformatted, pyright 31/1). Reformatting 25
     unrelated files would bury the behavior-unchanged import diff. Whole-repo counts held flat;
     touched files made clean. Every phase gate obeyed this.

[D2] Committed to main, committing the RED state first (e10ee9d). Repo had ZERO commits; main is
     the natural home; the tag/bisect/rollback design needs linear history. Rollback = git reset
     --hard <tag>.

[D3] Never-import-your-own-root; facades use eager exports. `from app.features.<f> import X` inside
     <f> raises ImportError on a half-built package. Rule (c) of the checker guards it permanently.

[D4] ingestion facade re-exports `normalization` as a MODULE, not loose symbols. Call sites use
     norm.Block / norm.normalize_body (numpy.linalg pattern) — keeps the call-site diff to one line.

[D5] platform/db gets NO facade. ~40 ORM classes/enums/DDL helpers = large intrinsically-namespaced
     vocabulary; one root = god-module, destroys db.enums.X vs db.models.Y disambiguation. Standard
     mandates entrypoints for FEATURES, not platform sub-capabilities. Import by full path.

[D6] app.* startup import graph = 46 (was 45). +1 = the app.shared namespace from the Phase-2
     hashing relocation. No feature facade leaks into app.main's startup graph.

[D7] platform/clients export set is 9, not the plan's 7. Call sites revealed contextualizer also
     imports AnthropicError + cached_system_block. Always gather the real set from call sites.

═════ RESUME PROMPT (for the NEXT session — the refactor is done) ═════

The 8-phase architecture-conformance refactor is COMPLETE and committed (main, tip cfdb503). The
backend is fully conformant: every feature + platform/clients is behind a public __init__.py facade,
zero external deep-imports remain, and tools/check_feature_boundaries.py enforces the 4 boundary
rules in `make check`. Governing docs: root CLAUDE.md, docs/adr/0003, MASTER-HANDOVER.md.

There is NO refactor work left. The next work is PRODUCT: Phase 4 (RAG agent + Next.js chat UI).

Before Phase 4:
  1. Live Confluence service account (current token is dead — the account has no Confluence seat;
     a fresh token alone won't fix it). Set CONFLUENCE_SPACES. See MASTER-HANDOVER §B.3.
  2. First real ingest of 1–2 live pages (rebuild the dev DB first: make down && make up && make
     migrate — it may carry the old non-deferrable FK). Everything Phase-3 was verified on the Fake
     provider + fixture corpus only.
  3. Rotate the four keys pasted into chat (OpenAI, Anthropic, Cohere, Confluence).

Phase 4 backend work is a NEW `rag_agent` feature behind its own public root, per the standard.
Wire the staged Cohere reranker, replace the fixture-backed permission policy with real ACL storage,
replace the apps/web /api/chat 501 stub, publish the chat contract in packages/contracts. It is an
HTTP + LLM surface → apply securing-http-and-llm-endpoints. UI voice/identity → the omniboost-brand
skill. Full task list in MASTER-HANDOVER §E.

STANDING GATE (run from apps/automation after any change; all via `uv run`; dev tools via `uv sync --extra dev`):
  make check                                   -> boundaries + pytest (the enforced gate)
  uv run python tools/check_feature_boundaries.py  -> exit 0
  uv run pytest -q                             -> 99 passed
  uv run pytest --collect-only -q | grep :: | sort  -> byte-identical to scratchpad/baseline/nodeids_ref.txt
  import cycle proof:  uv run python -c "import app.features.evaluation, app.features.retrieval, app.features.ingestion, app.features.confluence_sync, app.main"  -> silent exit 0
  import-graph:  uv run python -c "import app.main,sys;print(len([m for m in sys.modules if m.startswith('app.')]))"  -> 46
  OpenAPI byte-identical to scratchpad/baseline/openapi.json (info.version 0.2.0)
  no-regression:  ruff check . == 2 errors · ruff format --check . <= 25 files · pyright == 31 errors/1 warning  (D1)

KEY FILES:
  - apps/automation/tools/check_feature_boundaries.py — the boundary gate (4 rules, stdlib ast)
  - apps/automation/app/features/*/__init__.py + app/platform/clients/__init__.py — the 5 facades
  - apps/automation/app/features/confluence_sync/application/worker.py — lazy reconciliation import: DO NOT HOIST
  - CLAUDE.md (root) — project standard · docs/adr/0003 — decisions · .compact-ultra/MASTER-HANDOVER.md — full state
  - apps/automation/app/__init__.py — __version__ single source
  - /private/tmp/claude-501/.../scratchpad/baseline/ — invariants (nodeids_ref.txt, openapi.json)

RUN COMMANDS (Makefile at repo ROOT; $(AUTOMATION)=apps/automation):
  make up · make migrate · make test · make eval · make boundaries · make check · make web-dev
  test: cd apps/automation && uv run pytest -q    types: uv run pyright    lint: uv run ruff check .
  web build: pnpm --filter web build
  lang: Python 3.12 (uv 0.12.1, ruff 0.16.1, pyright, hatchling; dev tools in the `dev` extra) · Node/pnpm 10

ENV (keys, not values):
  - Root .env (repo root, gitignored) — read by get_settings(); OPENAI_API_KEY (LIVE), ANTHROPIC_API_KEY
    (LIVE), RERANKER_API_KEY/Cohere (LIVE, staged), CONFLUENCE_API_TOKEN (DEAD — no seat), CONFLUENCE_SPACES (EMPTY),
    CONFLUENCE_WEBHOOK_SECRET (stray `changeme` — empty before live webhooks), DATABASE_URL, EMBEDDING_*
  - Tests use a HERMETIC settings fixture (independent of .env) + omniboost_rag_test DB. 99 passed regardless of .env.
  - DATABASE_URL -> Postgres omniboost_rag_pg on :5434.

CONVENTIONS:
  commit-style: conventional (refactor(scope): / fix: / chore: / feat: / docs:); footer
    "Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
  branch: main, no PR requested; one atomic commit per plan sub-step; gate before each commit
  gate: no-regression on pre-existing ruff/pyright dirt (D1), NOT zero

IN-FLIGHT EDIT: none (all committed, working tree clean)

REJECTED PATHS (don't redo):
  - Don't reformat the 25 pre-existing ruff-unformatted files / fix the 2 alembic import-sort errors — D1
  - Don't rename apps/automation/app/ -> src/ (breaks hatch/pyright/pytest/uvicorn/alembic)
  - Don't facade platform/db (D5); don't create packages/ui or packages/config (one consumer)
  - Don't hoist worker.py's function-local reconciliation import (import-time failure)
  - Don't run a bare `uv sync` (prunes ruff/pyright/pytest); use `uv sync --extra dev`

RUNNING / EXTERNAL:
  bg processes: none of ours. Docker Postgres omniboost_rag_pg :5434 must be up for pytest (DB conftest).
  ports: :5434 pg (this project), :5433 other pg
  external: OpenAI embeddings (live); Confluence token dead/needs a seat + rotation (not needed for tests —
    fixture gateway is offline).

═════════════════════════
